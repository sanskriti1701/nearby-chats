specVersion: "v1"
serviceId: "my-typescript-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-typescript-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
